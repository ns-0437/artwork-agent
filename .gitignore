.env
node_modules/
dist/
build/
__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/
*.db
*.sqlite
.DS_Store
*.tsbuildinfo

# LocalDisk's default dev storage dir (internal/storage - see .env.example's
# STORAGE_LOCAL_DIR) when running services.api-go outside Docker.
data/artifacts/

# Single-fixture debug runs (run_eval.py --fixture-id) - disposable, and
# deliberately routed here instead of evals/results/ so they can never
# collide with a real report file, frozen or otherwise.
evals/results/debug/

# IDE-local settings, not project config.
.vscode/
.idea/
